diff --git a/openfold/utils/script_utils.py b/openfold/utils/script_utils.py
index facc103..1bd39b4 100644
--- a/openfold/utils/script_utils.py
+++ b/openfold/utils/script_utils.py
@@ -9,7 +9,6 @@ import torch
 
 from openfold.model.model import AlphaFold
 from openfold.np import residue_constants, protein
-from openfold.np.relax import relax
 from openfold.utils.import_weights import (
     import_jax_weights_,
     import_openfold_weights_
@@ -230,6 +229,7 @@ def prep_output(out, batch, feature_dict, feature_processor, config_preset, mult
 
 
 def relax_protein(config, model_device, unrelaxed_protein, output_directory, output_name, cif_output=False):
+    from openfold.np.relax import relax
     amber_relaxer = relax.AmberRelaxation(
         use_gpu=(model_device != "cpu"),
         **config.relax,
