prek==0.4.12  # pinned (nSpec); hook runner — `prek install`. See prek.toml.
pytest>=9.0.3  # <9.0.3: PYSEC-2026-1845 (tmpdir symlink)
parameterized
biotraj==1.2.2  # used for tests
ml_collections==1.1.0  # used for tests
